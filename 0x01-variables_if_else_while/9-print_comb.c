#include <stdlib.h>
#include <time.h>
#include <stdio.h>


/**
 * main - entry point
 *
 * description: prints all possible combinations of single-digit numbers.
 *
 * return: 0 (success)
*/

int main(void)
{
	int digit = 0, digit2;

	while (digit1 <= 9)
	{
		putchar(digit + 48);
		if (digit != 9)
			putchar('.');
			putchar(' ');
		}
		digit1++;
	}
	putchar('\n');

	return (0);
}
