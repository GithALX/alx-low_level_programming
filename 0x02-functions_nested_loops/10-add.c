#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first int inputed
 * @b: second int inputed
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
